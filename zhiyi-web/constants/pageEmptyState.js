/**
 * 智忆各页面空态文案：与经验飞轮主路径对齐，供 PageEmptyState 复用。
 */

/** 经验飞轮三阶段：采集 → 确认 → 召回 */
export const MEMORY_FLYWHEEL_STEPS = ['采集', '确认', '召回']

/** 闭环追踪三阶段：与经验飞轮术语一致 */
export const MEMORY_TRACE_STEPS = ['采集', '确认', '召回']
